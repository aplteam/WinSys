 R←Test_ShellExec_002(stopFlag batchFlag);⎕TRAP;parms
⍝ Exercise `##.WinSys.ShellExecute` with a parameter space and an invalid verb
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 parms←##.WinSys.CreateParms_ShellExecute
 parms.file←↑#.GetCommandLineArgs
 parms.verb←'DOES_NOT_EXIST'
 :Trap 0
     ##.WinSys.ShellExecute parms
 :Else
     →T.PassesIf'Invalid verb'≡↑⎕DM
 :EndTrap

 R←T._OK
