 R←Test_ShellExec_003(stopFlag batchFlag);⎕TRAP
⍝ Exercise `##.WinSys.ShellExecute` with an invalid right argument
 ⎕TRAP←(999 'C' '. ⍝ Deliberate error')(0 'N')
 R←T._Failed

 :Trap 0
     ##.WinSys.ShellExecute'one' 'two'
 :Else
     →T.PassesIf'Invalid right argument'≡↑⎕DM
 :EndTrap

 R←T._OK
